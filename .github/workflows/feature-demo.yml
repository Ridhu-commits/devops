name: Feature Demo Workflow

on:
  push:
    branches:
      - feature-demo

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Touch feature.md
        run: touch feature.md

      - name: List files
        run: ls -la